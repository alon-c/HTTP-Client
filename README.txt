Computers Networking Application Course – HTTP client
1. client.c
implemintation of a HTTP client that constructs an HTTP request based on user’s command line input, sends 
the request to a Web server, receives the reply from the server, and displays the reply 
message on screen.
note: this http client supports ipv4 connections only.